---
name: "[Tests] Unit Tests"
description: "Run unit tests"

# This workflow runs three independent test suites:
# 1. run-tests: General project tests (excludes RAG and ArgoCD MCP tests)
# 2. run-argocd-tests: ArgoCD MCP tests (requires ARGOCD_API_URL env var)
# 3. run-rag-tests: RAG module tests (requires PyTorch and special setup)
# Each suite runs independently to isolate dependencies and environment requirements.

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    paths:
      - 'ai_platform_engineering/**'
      - 'tests/**'
      - 'pyproject.toml'
      - 'uv.lock'
      - 'Makefile'

permissions:
  contents: read
  actions: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  run-tests:
    if: github.actor != 'github-actions[bot]' && github.actor != 'caipe-ci-release[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
      - name: Install uv
        uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
        with:
          python-version: "3.13"
          enable-cache: true

      - name: Install dependencies and run tests
        run: |
          echo "Installing dependencies with uv sync..."
          uv sync --group dev --group unittest --frozen

          echo "Running tests with coverage..."
          uv run pytest \
            --ignore=integration \
            --ignore=evals \
            --ignore=ai_platform_engineering/knowledge_bases/rag/tests \
            --ignore=ai_platform_engineering/mcp/argocd/tests \
            --ignore=ai_platform_engineering/mcp/komodor/tests \
            --ignore=ai_platform_engineering/autonomous_agents/tests \
            --ignore=volumes \
            --ignore=docker-compose \
            --cov=ai_platform_engineering \
            --cov-report=xml \
            --cov-report=html

      - name: Upload coverage reports
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: coverage-reports-main
          path: |
            coverage.xml
            htmlcov/
        continue-on-error: true

  run-argocd-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
      - name: Install uv
        uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
        with:
          python-version: "3.13"
          enable-cache: true

      - name: Run ArgoCD MCP tests
        run: |
          uv venv
          source .venv/bin/activate && uv sync --no-dev
          cd ai_platform_engineering/mcp/argocd && make test

  # run-rag-tests:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
  #       uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v6
  #         python-version: "3.13"

  #     - name: Install UV
  #       run: |
  #         curl -LsSf https://astral.sh/uv/install.sh | sh

  #     - name: Run RAG tests
  #       run: |
  #         /home/runner/.local/bin/uv venv
  #         source .venv/bin/activate && cd ai_platform_engineering/knowledge_bases/rag/server && uv sync --dev
  #         source .venv/bin/activate && uv add --extra-index-url https://download.pytorch.org/whl/cpu --index-strategy unsafe-best-match torch --force-reinstall
  #         make test-rag-all

  #     - name: Upload RAG coverage reports
  #       if: github.event_name == 'pull_request'
  #       uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
  #         name: coverage-reports-rag
  #         path: |
  #           ai_platform_engineering/knowledge_bases/rag/server/coverage.xml
  #           ai_platform_engineering/knowledge_bases/rag/server/htmlcov/

  run-autonomous-agents-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v6
        with:
          python-version: "3.13"
      - name: Install UV
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh

      - name: Run autonomous-agents tests
        run: |
          cd ai_platform_engineering/autonomous_agents
          /home/runner/.local/bin/uv venv
          /home/runner/.local/bin/uv sync --extra dev
          /home/runner/.local/bin/uv run pytest --cov=autonomous_agents --cov-report=xml -v

      - name: Upload coverage reports
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: coverage-reports-autonomous-agents
          path: |
            ai_platform_engineering/autonomous_agents/coverage.xml
        continue-on-error: true

